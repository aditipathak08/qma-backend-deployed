version: '3.8'

services:
  redis:
    image: redis:alpine
    container_name: quantity-redis
    ports:
      - "6379:6379"

  api:
    build:
      context: .
      dockerfile: Dockerfile.api
    container_name: quantity-api
    ports:
      - "5000:80"
    environment:
      - ASPNETCORE_ENVIRONMENT=Development
      - DATABASE_URL=postgresql://quantity_measurement_db_6fs1_user:BFz0SMmO4MyM17cclwwTuXc1VN37qiKc@dpg-d7fl2c7lk1mc73dghiq0-a/quantity_measurement_db_6fs1
      - Redis__ConnectionString=redis:6379
      - Jwt__Key=THIS_IS_A_SECURE_32_CHARACTER_KEY_!!!
    depends_on:
      - redis

volumes:
  sqlvolume:
